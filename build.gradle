apply plugin: 'groovy'
apply plugin: 'idea'

dependencies {
    compile gradleApi()
    groovy localGroovy()
}

apply plugin: 'maven'

dependencies {
    testCompile('junit:junit:4.11')
}

repositories {
    mavenCentral()
}

group = 'com.thoughtworks'
version = '0.1-SNAPSHOT'
